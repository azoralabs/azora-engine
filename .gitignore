# Packaged library bundles (rebuild with tools/package.sh)
dist/
*.azlib

# Native runtime build output (rebuild with runtime/build.sh)
runtime/build/
*.dylib
*.so
*.dll

# Per-project build output (created by tools/build.sh in engine projects)
.azora-build/

# Generated LLVM IR / object files
*.ll
*.o

# OS noise
.DS_Store
